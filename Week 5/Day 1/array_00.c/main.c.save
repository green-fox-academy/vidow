#include <stdio.h>
#include <stdlib.h>

/*
 * Create an array of 5 integers.
 * Fill it with random integer numbers.
 * Print out the array elements in one line, divided by " | ".
 * Reverse the order of the numbers _in_the_array_ an print them out again.
 */

int main()
{

    int a_numbers[5] = {5, 12, 17, 21, 28};
    int lenght = sizeof(a_numbers) / sizeof(0);

    for (int i = 0; i < lenght; i++) {

       printf("%d | ", a_numbers[i]);
        
    }
    
    for (int i = 0; i < sizeof(a_numbers[i]) / sizeof(0); i++)

        printf("%d | ", a_numbers[j]);

    }

    return 0;
}
