#include <stdio.h>
#include <stdlib.h>

//fill abc string from a to z, print out

int main()
{
    char alphabet[25] = {"abcdefghijklmnopqrstuwxyz"};

    for (int i = 0; i <= sizeof(alphabet) / sizeof(alphabet[0]; i++) {

        printf("%c\n", alphabet[i]);
    }

    return 0;
}
