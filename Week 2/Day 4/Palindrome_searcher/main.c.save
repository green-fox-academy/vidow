/*
2XP

What the hell is a palindrome?

Exercise

Create a function named search palindrome following your current language's style guide.
It should take a string, search for palindromes that at least
3 characters long and return a list with the found palindromes.

Examples

input	output
"dog goat dad duck doodle never"	["og go", "g g", " dad ", "dad", "d d", "dood", "eve"]
"apple"	[]
"racecar"	["racecar", "aceca", "cec"]
""	[]

*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    char word[255];
    int temp = 0;

    printf("Enter some words for the Palindrome searcher: ");

    scanf("s", &word);

        for (int i = 0; i < strlen(word) - 1; i++){
            for (int j = i + 1; j < strlen(word); j++){

/*                if (word[i] > word[j]){

                temp = word[i];
                word[i] = word[j];
                word[j] = temp;

                }
 */           }
        }

    printf("c", &word);



    return 0;
}
