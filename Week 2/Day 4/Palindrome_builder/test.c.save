#include <stdio.h>
#include <stdlib.h>

int main()
{
    char input[255];
    char output[255];
    char temp = 0;

        puts(("Enter a word: ");
        gets(word);

    for (int i = 0; i < input / 2, i++);
        temp = str[i]
    
        
/*
    for (int i = 0; i < input - 1; i++){

        for (int j = i + 1; i < input; j++){

            if (input[i] > input[j]){

                temp = input[i];
                input[i] = input[j];
                input[j] = temp;

            }
        }
    }
*/
/*
return 0;
}

void reverse(char str[]) {

    int i;
    char c;
    int len = strlen(str);

    for (i=0; i< len/2; i++) {
        c = str[i];
        str[i] = str[len - i - 1];
        str[len - i - 1] = c;
    }

    printf("%s", c);

return;
}
*/
