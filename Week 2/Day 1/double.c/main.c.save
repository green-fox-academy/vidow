#include <stdio.h>
#include <stdlib.h>
#include "double.h"

// create a function called `doubling` that doubles it's input parameter
// again the parameters value should be stored in a .h file


int main()
{
    dd();
    dd(4);

    return 0;
}

void dd(int NUMBER, NUMB)
{
    printf("If we double %d the result is", NUMBER);

    printf(" %d.\n", NUMBER * 2);

    return;
}
