#include <stdio.h>
#include <stdlib.h>
#include "gf.h"

// define a variable and assign the value `Greenfox` to it in a .h file
// create a function called `greet` that greets it's input parameter
// define more names to greet, call the function multiple times

int main()
{

    greet(a, b, c, d, e);

    return 0;
}

void greet(int name1, int name2, int name3, int name4, int name5, int name6)
{
    printf("Greetings, %s or %s or %s!\n", name1, name2, name3, name4, name5);
    return;
}
